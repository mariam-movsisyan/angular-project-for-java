export interface IUser {
    name: string;
    surname: string,
    username: string;
    password: string;
    confirmPassword: string;
}
export interface User {
    username: string;
    password: string;
}